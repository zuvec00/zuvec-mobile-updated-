import 'package:flutter/material.dart';

class Crocs extends StatelessWidget {
  const Crocs({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [Center(child: Text('C R O C S'))],
    );
  }
}
